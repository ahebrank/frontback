description "uWSGI server instance configured to serve issue_proxy"

start on runlevel [2345]
stop on runlevel [!2345]

setuid user
setgid www-data

chdir /usr/local/frontback/endpoint
exec uwsgi --ini frontback.ini